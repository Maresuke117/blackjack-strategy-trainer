🗄️ データベース設計書（ローカルデータ保存）
アプリ内で保存・管理するデータは、大きく分けて「① ユーザーの設定・ステータス情報」と「② 収支の履歴データ」の2つのテーブル（データのまとまり）で構成します。

1. Settings テーブル（ユーザー設定・ステータス）
現在の所持金や、アプリ内でユーザーが選んだ設定を保存しておく場所です。アプリを再起動した際に、前回の状態を復元するために使います。データは1行（1レコード）のみを常に上書き更新していきます。

カラム名（英語）	データ型	説明	初期値（例）
id	整数 (int)	管理用のID（常に 1）	1
current_balance	整数 (int)	現在の所持金（チップ残高）	1000
base_bet_amount	整数 (int)	選択中のベース賭け金（10, 30, 50, 100）	10
betting_system	文字列 (String)	選択中のベット手法（flat, martingale, fibonacci など）	"flat"
is_soft_hand_mode	真偽値 (bool)	ソフトハンドのみモードのON/OFF	false (OFF)
is_total_visible	真偽値 (bool)	合計値表示のON/OFF	true (ON)
2. BalanceHistory テーブル（収支履歴データ）
シミュレーションモードでゲームをプレイするたびに、その時点での「所持金」を記録し続けるテーブルです。このデータを使って、収支画面の「折れ線グラフ」を描画します。1ゲーム終わるごとに、新しいデータが1行ずつ追加されていきます。

カラム名（英語）	データ型	説明	備考
id	整数 (int)	履歴の通し番号（自動で1ずつ増える）	グラフのX軸（プレイ回数）として利用
balance	整数 (int)	そのゲーム終了時点での所持金	グラフのY軸として利用
played_mode	文字列 (String)	どのモードで遊んだか（standard, switch, free_bet）	後でモードごとの成績を見分けるため
created_at	日時 (DateTime)	プレイした日時	履歴の並び替え用
💡 実装に関するアドバイス（Flutterでのアプローチ）
この設計をFlutterのコードに落とし込む場合、以下のツール（パッケージ）を使うのが初心者の方にも扱いやすく、おすすめです。

Settings テーブルの保存:
shared_preferences というパッケージを使います。これは「所持金は1000」「合計値表示はON」というような、単純な名前と値のペア（Key-Value）を保存するのに最適な軽いツールです。

BalanceHistory テーブルの保存:
sqflite（スマホ用の軽量なSQLデータベース）や、Hive（よりモダンで高速なNoSQLデータベース）というパッケージを使います。グラフを描画するためには「過去100回分のデータだけを取り出す」といった処理が必要になるため、履歴データはこちらに保存してリストとして引き出すのがスマートです。

📐 ブラックジャック アプリ詳細設計書
1. クラス・オブジェクト詳細設計（疎結合なデータとロジック）
各クラスが「自分の仕事だけ」に責任を持つ（単一責任の原則）ように分け、お互いに依存しすぎない「疎結合」な設計にします。

データモデル（純粋なデータの箱）

PlayingCard クラス: 1枚のカード（スート、ランク、値）。

Hand クラス: 配られたカードのリストを持ち、合計点数（Aの1と11の自動計算含む）や、バスト・ブラックジャックの判定を行う。プレイヤー用とディーラー用で使い回せます。

ロジック（処理を行うクラス）

Deck クラス: PlayingCard のリストを持ち、シャッフルや「カードを1枚引く（pop）」処理のみを担当。

BettingSystem クラス: これまでの勝敗履歴を受け取り、選択された手法（マーチンゲールやフィボナッチ等）に基づいて「次のベット額」だけを計算して返すクラス。

StrategyEvaluator クラス: プレイヤーの Hand とディーラーのアップカードを受け取り、「正解のアクション（StringやEnum）」を返す判定専用クラス。

ゲーム進行（オーケストレーター）

GameEngine クラス: 上記のクラスを組み合わせてゲームを進行させます。例えば「Deckからカードを引き、Handに追加し、勝敗が決まったらBettingSystemを呼び出す」という進行管理のみを行います。

2. 状態管理の設計（Riverpodの採用）
Flutterの状態管理パッケージの中で、現在最も推奨されていて、かつ「疎結合」な設計と非常に相性が良い flutter_riverpod（リバーポッド） を採用します。

GameStateProvider: 現在のゲームの進行状況（プレイ中、結果発表中など）や、プレイヤー・ディーラーの Hand 情報を保持・更新します。ゲーム画面はこのプロバイダーだけを監視します。

SettingsProvider: データベース（SharedPreferences）と連携し、ユーザーの所持金や「ソフトハンドモードのON/OFF」「選択中のベット手法」を保持します。

BalanceHistoryProvider: データベース（SQLite等）と連携し、収支グラフを描画するための履歴データのリストを管理します。

※Riverpodを使うことで、「ゲーム画面」から「所持金」を更新した瞬間、自動的に「収支グラフ画面」や「設定画面」の表示も最新に切り替わるようになります。

3. UIコンポーネント設計（再利用可能なウィジェット）
画面の見た目をレゴブロックのように細かく分け、どのモード（通常、スイッチ、フリーベット）でも使い回せるようにします。

PlayingCardWidget: PlayingCard オブジェクトを1つ受け取り、トランプの画像（または枠と数字）を描画する部品。

HandWidget: Hand オブジェクトを受け取り、複数枚の PlayingCardWidget を少しずつズラして扇状（または横並び）に表示する部品。合計値の表示/非表示もここで制御します。

ActionButtonsWidget: ヒット、スタンド、ダブル、スプリットなどのボタンをまとめた部品。現在のゲーム状態（スプリットできるか等）を受け取り、押せないボタンはグレーアウトさせます。

BalanceBoardWidget: 画面上部に常に表示される「現在の所持金」と「今回のベット額」を表示するパネル部品。

この設計に沿って作れば、後から「新しいベット手法を追加したい」と思った時も BettingSystem クラスを少し触るだけで済みますし、「カードの見た目を変えたい」時も PlayingCardWidget だけを修正すれば全画面に反映される、非常に美しいコードになります！